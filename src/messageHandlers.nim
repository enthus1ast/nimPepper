proc masterHandleLog() = 
  discard
  # perform validation
  # write to given logfile/folder
  # [optional] call log file handler

  # create logResp
  #   fill in the id from the old msg
  # answer with logResp

